# Streamlit Cloud Secrets Configuration
# Copy this file to secrets.toml and fill in your API keys
# DO NOT commit secrets.toml to git!

# OpenAI API Key (for GPT models)
OPENAI_API_KEY = "sk-..."

# Anthropic API Key (for Claude models)
ANTHROPIC_API_KEY = "sk-ant-..."

# Google API Key (for Gemini models)
GOOGLE_API_KEY = "..."

# Optional: Ollama API Base URL (for local models)
# OLLAMA_API_BASE = "http://localhost:11434"

# Optional: Email Configuration (for notifications)
# SMTP_USERNAME = "your.email@gmail.com"
# SMTP_PASSWORD = "your-app-specific-password"
# SMTP_PROVIDER = "gmail"  # or "outlook" or "custom"

# Note: In Streamlit Cloud, configure these secrets in the app settings
# under "Secrets" section. The format is TOML.
